# Handoff: 2026-04-29T17-54-00

## Ledger

### Goal
Implement the bridge foundation milestone: persistent pending bridge table, deduplicated ingestion, and wired BridgeLinkEngine.

### Now
- **Revisited OLAP database choice** — decided to keep ClickHouse (not DuckDB). Rationale: materialized views are load-bearing for trending contracts; switching cost exceeds benefit at current scale.
- **Implemented na-5hcn**: Postgres `pending_bridge_outs` table with retry scheduling and 30-day expiry. Schema + indices created.
- **Implemented na-snr2**: ClickHouseSink now batch-deduplicates by `event_id` before insert. Prevents duplicate ingestion on overlapping lookback windows.
- **Implemented na-5p48**: `BridgeLinkEngine` refactored to use `PendingBridgeStore` (Postgres or in-memory). Key fix: cross-chain depositId collisions prevented by matching on `(link_key, link_key_type, src_chain)` with `src_chain != dst_chain` filter.
- **All new tests passing**:
  - `core/tests/standalone/test_pending_bridge.py` — 9 passed
  - `core/tests/standalone/test_dedup_sink.py` — 5 passed
  - `ops/dagster/tests/test_dagster_pipeline.py` — 3 passed
- **Pre-existing test failure noted**: `test_clickhouse_sink.py::TestEventToRow::test_decimal_fields` fails because `_event_to_row` serializes Decimals to strings, but test expects Decimal objects. Not caused by this session's changes.

### Blockers
- None

### Decisions
- **Keep ClickHouse** for OLAP. DuckDB deferred to Phase 2 if OSS adopters complain.
- **Pending store pattern**: `PendingBridgeStore` ABC with `InMemoryPendingBridgeStore` (tests/local dev) and `PostgresPendingBridgeStore` (production).
- **Retry cadences**: Across = 2min, canonical bridges = 6hr, default = 10min.
- **Cross-chain collision fix**: Match candidates by `link_key` + `link_key_type`, then filter to `src_chain != dst_chain`. Earliest `src_block_time` wins on ties.

### Next
1. **Apply Postgres migration**: run `core/schemas/pending_bridge_outs.sql` against the Postgres container.
2. **Run pipeline against live data**: trigger Dagster `decoded_events` + `bridge_links` assets, verify pending rows appear in Postgres.
3. **Validate Across matching**: sample 50 Across deposits on Base→Ethereum, confirm fills match and depositId=1 on Base is distinct from depositId=1 on Ethereum.
4. **Fix pre-existing test**: `test_decimal_fields` in `test_clickhouse_sink.py` — `_event_to_row` returns strings for Decimals but test asserts Decimal objects.

## Files Changed
- `core/schemas/pending_bridge_outs.sql` (new)
- `core/identity/pending_bridge.py` (new)
- `core/identity/bridge_links.py` (modified)
- `core/sink/clickhouse.py` (modified)
- `core/pyproject.toml` (modified — added psycopg2-binary)
- `ops/dagster/nexus_pipeline/resources.py` (modified — added PostgresResource)
- `ops/dagster/nexus_pipeline/assets.py` (modified — wired pending store)
- `ops/dagster/nexus_pipeline/__init__.py` (modified — registered postgres resource)
- `ops/dagster/tests/test_dagster_pipeline.py` (modified)
- `core/tests/standalone/test_pending_bridge.py` (new)
- `core/tests/standalone/test_dedup_sink.py` (new)

## Session Info
- Duration: ~1.5 hours
- Tickets closed: na-5hcn, na-snr2, na-5p48
- Test coverage: 17 new tests, all passing
- Key discovery: `BridgeLinkEngine` cross-chain collision bug is fixed by the `src_chain != dst_chain` filter
