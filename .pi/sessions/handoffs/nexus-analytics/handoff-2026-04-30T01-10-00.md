# Handoff: 2026-04-30T01-10-00

## Ledger

### Goal
Fix test failures and verify the bridge foundation milestone works end-to-end with live data.

### Now
- **Fixed `test_decimal_fields`** — test now expects strings (matches `_event_to_row` behavior for ClickHouse)
- **Applied Postgres migration** — `pending_bridge_outs` table with 3 indices live in `nexus_ops` database
- **Created `canonical_logs` ClickHouse table** — was missing from schema
- **Fixed `PostgresResource.database`** — changed from `nexus` to `nexus_ops` (matches docker-compose)
- **Fixed `mark_matched` NOT NULL violation** — removed `next_retry_at = NULL`, matched rows keep original timestamp
- **Fixed HyperSync API usage**:
  - Changed `bearer_token` → `api_token` (required by current client version)
  - Added `proactive_rate_limit_sleep=True` to ClientConfig
  - Added `await client.wait_for_rate_limit()` before each query
  - Added `max_num_blocks=100` and `max_num_logs=50000` per chunk for pagination
- **Verified end-to-end pipeline** — 21,618 raw logs → 9,355 decoded events from 1 minute of Base
- **All 25 tests passing**

### Blockers
- HyperSync free tier has aggressive rate limits (Peak RPM: 3). Works but requires patience between queries. Production use needs paid tier.

### Decisions
- **Keep ClickHouse** for OLAP. Materialized views are load-bearing.
- **Postgres for operational state** — `pending_bridge_outs` table with retry scheduling
- **`api_token` not `bearer_token`** — HyperSync client now requires `api_token` parameter
- **Chunked HyperSync queries** — 100 blocks per chunk, 50k logs max, with explicit rate limit waits
- **`mark_matched` keeps `next_retry_at`** — avoids NOT NULL violation; timestamp is harmless on matched rows

### Next
1. Run full pipeline with Dagster (needs HyperSync rate limit cooldown or paid tier)
2. Validate Across bridge matching with real Base→Ethereum data
3. Consider adding exponential backoff or circuit breaker for HyperSync rate limits

## Files Changed
- `core/tests/standalone/test_clickhouse_sink.py` — fixed `test_decimal_fields` assertion
- `core/identity/pending_bridge.py` — fixed `mark_matched` SQL (removed `next_retry_at = NULL`)
- `core/adapters/evm/__init__.py` — fixed HyperSync API usage (api_token, rate limiting, pagination)
- `ops/dagster/nexus_pipeline/resources.py` — fixed PostgresResource database name

## Session Info
- Duration: ~1.5 hours
- Tests: 25/25 passing
- Key discovery: HyperSync client changed from `bearer_token` to `api_token`; free tier needs explicit rate limit handling
